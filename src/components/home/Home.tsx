import React, { ReactElement } from 'react';

/**
 * This is the component for the Home page. Placeholder.
 * Will eventually have functionality in later versions.
 *
 * @returns - {ReactElement} - component for Home page
 */
export function Home(): ReactElement {
    return <h1 data-testid="home-id">Home</h1>;
}
